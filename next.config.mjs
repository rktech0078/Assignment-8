/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io', 'img.daisyui.com'], // Add Sanity's CDN domain here
      },
};

export default nextConfig;
